function ProfileImage({className}) {
    return (
        <div className={`dark-shade rounded-[15px] row-span-4 col-span-4 ${className}`} >
            Profile Image
        </div>
    );
}

export default ProfileImage;